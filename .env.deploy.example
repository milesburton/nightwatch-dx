# Copy this to .env.deploy and fill in your values.
# .env.deploy is gitignored — never commit real values.

# SSH target: user@host
SERVER=user@192.168.x.x

# Path to the repo on the remote server
REPO=~/code/dx-watch
